
@{
    ViewBag.Title = @Model.Name;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>
<ul>
    <li>Membership Type : @Model.MembershipType.Name</li>
    @if (Model.BirthDate != null)
    {
        <li>BirthDate : @Model.BirthDate.ToString("MM/dd/yyyy")</li>
    }
</ul>

